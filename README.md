# Assignments_1
# Assignment 1 – Python Greeting Program

This repository contains a simple Python program that takes the user's **first name** and **last name** as input and prints a personalized greeting message.

---

## 📌 Features

- Prompts the user to enter their first name.
- Prompts the user to enter their last name.
- Displays a friendly welcome message in the format:
---

## 🧠 How It Works

The program performs the following:

1. Takes user input using `input()`
2. Stores the names in variables
3. Concatenates them into a greeting string
4. Prints the final message to the console

---

## 🚀 Running the Program

### **Prerequisites**
- Python installed (version 3.x recommended)

### **Steps to Run**

```bash
python Assignmet1.py
Enter first name:
Enter last name:
first_name = str(input("Enter first name: "))
last_name = str(input("Enter last name: "))
print("Hello " + first_name + " " + last_name + "!" + "Welcome to the python program")
/project-folder
│── Assignmet1.py
│── README.md
